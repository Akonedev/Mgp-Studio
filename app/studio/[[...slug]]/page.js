import StandaloneShell from '@/components/StandaloneShell';

export const metadata = {
  title: 'Studio — Mgp Studio (Môguô Puissant)',
};

export default function StudioPage() {
  return <StandaloneShell />;
}
